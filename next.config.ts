import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["sjc.microlink.io"], // Add the hostname from your error here
  },
  /* config options here */
};

export default nextConfig;
